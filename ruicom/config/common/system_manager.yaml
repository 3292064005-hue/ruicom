health_topic: recon/health
health_typed_topic: recon/health_typed
mission_state_typed_topic: recon/mission_state_typed
control_command_topic: recon/system_manager/command
manager_state_topic: recon/system_manager/state
publish_diagnostics: true
diagnostics_topic: /diagnostics
required_nodes:
- vision_counter_node
- mission_manager_node
- mission_recorder_node
- cmd_safety_mux_node
- platform_bridge_node
readiness_requirements:
  vision_counter_node:
  - camera_topic_bound
  - camera_frame_fresh
  - detector_contract_satisfied
  - detector_manifest_grade_satisfied
  - field_asset_ready
  - vendor_runtime_contract_satisfied
  - vendor_bundle_preflight_satisfied
  mission_manager_node:
  - route_bound
  - navigation_contract_satisfied
  - field_asset_ready
  - navigation_runtime_probe_satisfied
  - vendor_runtime_contract_satisfied
  - vendor_bundle_preflight_satisfied
  mission_recorder_node:
  - writer_ready
  cmd_safety_mux_node:
  - command_path_bound
  - output_feedback_required_satisfied
  platform_bridge_node:
  - command_path_bound
  - command_flow_contract_satisfied
  - runtime_interface_contract_satisfied
  - feedback_contract_satisfied
  - platform_runtime_probe_satisfied
  - vendor_runtime_contract_satisfied
  - vendor_bundle_preflight_satisfied
ready_timeout_sec: 10.0
health_freshness_sec: 3.0
publish_rate_hz: 5.0
auto_activate: true
time_source_mode: ros
runtime_grade: integration
